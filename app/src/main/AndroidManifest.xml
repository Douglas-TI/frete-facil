<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.douglasti.fretefacil">

    <application
        android:name=".util.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreteFacil">
        <activity
            android:name=".mvp.view.vehicle.vehicleAdd.VehicleAddActivity"
            android:exported="false"
            android:parentActivityName=".mvp.view.vehicle.VehicleManageActivity"
            android:label="@string/vehicle_registration" />
        <activity
            android:name=".mvp.view.vehicle.VehicleManageActivity"
            android:exported="false"
            android:parentActivityName=".mvp.view.MenuActivity"
            android:label=""/>
        <activity
            android:name=".mvp.view.MenuActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>